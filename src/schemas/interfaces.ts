export interface User {
    firstName: string;
    lastName: string;
    email: string;
    // services: string[];
}
  
export interface Service {
    google: boolean;
    web_development: boolean;
    social_media: boolean
}